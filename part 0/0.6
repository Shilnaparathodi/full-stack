sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes a note and clicks Save

    Note right of browser: JavaScript prevents default form submit

    browser->>server: POST /exampleapp/new_note_spa (note data as JSON)
    activate server
    server-->>browser: 201 Created (saved note)
    deactivate server

    Note right of browser: Browser updates the notes list using DOM without page reload
